<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:usesCleartextTraffic="true">
        <activity android:name=".InstrumentQuizResultActivity"></activity>
        <activity android:name=".InstrumentQuizIntroActivity" />
        <activity android:name=".OXquizResultActivity" />
        <activity android:name=".OXquizStageActivity" />
        <activity android:name=".OXquizIntroActivity" />
        <activity android:name=".SelectLogSignActivity" />
        <activity android:name=".UserSetActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".IntroLastActivity" />
        <activity android:name=".NameSelect" />
        <activity android:name=".IntroActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>